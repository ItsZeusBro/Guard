class Terminal{
    constructor(thing){
        this.thing = this.interpret(thing)
    }
    interpret(){
        //should return a terminal object that we can rely on to be the same for all cases 
    }
}